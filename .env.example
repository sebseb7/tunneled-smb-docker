# SSH Connection Settings
SSH_HOST=your.remote.server.com
SSH_USER=your_username
SSH_PORT=22

# SSH Key Path (inside container)
SSH_KEY_PATH=/home/tunneluser/.ssh/id_rsa

# Remote SMB Settings
REMOTE_SMB_HOST=localhost
REMOTE_SMB_PORT=445

# Local SMB Settings
LOCAL_SMB_PORT=445

# SSH Options (optional)
# SSH_OPTIONS=-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ServerAliveInterval=60

# Example configurations:
# 
# Connect to SMB share on the same server as SSH:
# SSH_HOST=myserver.com
# SSH_USER=myuser
# REMOTE_SMB_HOST=localhost
# 
# Connect to SMB share on different server via SSH tunnel:
# SSH_HOST=jumphost.com
# SSH_USER=jumpuser
# REMOTE_SMB_HOST=fileserver.internal
# 
# Custom SSH port:
# SSH_PORT=2222 